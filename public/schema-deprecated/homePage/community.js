// 获取全部类型帖子
const allPosts = {
  type: "all",
};

// 获取领养类型帖子
const adoptionPost = {
  type: "adoption",
};

// 获取走失类型帖子
const lost_n_foundPost = {
  type: "lost-n-found",
};

// 获取交易类型帖子
const transaction = {
  type: "transaction",
};

// 获取交流问答类型帖子
const post = {
  type: "post",
};
